services:

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend

    ports:
      - "8000:8000"

    env_file:
      - .env

  frontend:
    build:
      context: ./frontend

    ports:
      - "8080:8080"

    depends_on:
      - backend